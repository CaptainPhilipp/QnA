- commentable = result.commentable
- question = commentable.is_a?(Question) ? commentable : commentable.question

h4 = link_to "Q: #{question.title}", question_path(question)
h5 = "Comment to #{commentable.class}:"
. = result.body
